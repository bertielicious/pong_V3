#include "config.h"
#include "set_address.h"

void render(struct pageData pageRender, struct pageData dataRender, struct pageData xRender, struct pageData yRender)
{
    
    // set_address( x, page);               //[row][col]
    // gameplay_area[page][x] = gameplay_area[page][x];
   //  lcdWrite(gameplay_area[page][x], HIGH);       // illuminate the pixel at coordinates x, y
    
}
